{
  "name": "pi-subagents",
  "version": "0.2.0",
  "description": "Synchronous delegation to isolated pi agents with explicit tool allowlists and bounded parallel read-only tasks",
  "type": "module",
  "keywords": ["pi-package"],
  "files": ["index.ts", "src", "README.md"],
  "pi": {
    "extensions": ["./index.ts"]
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*",
    "typebox": "*"
  },
  "engines": {
    "node": ">=22.19.0"
  }
}
